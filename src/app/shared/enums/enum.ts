export enum UserType {
  Employee = 'employee',
  CTO = 'cto',
  Manager = 'manager',
  Admin = 'admin',
}

export enum StateName {
  Common = 'common',
  Router = 'router',
  Dashboard = 'dashboard',
  Activity = 'activity',
  Profile = 'profile',
  Projects = 'projects',
  Report = 'report',
  Team = 'team',
}

export enum TaskStatus {
  Open = 'open',
  InProgress = 'in progress',
  Done = 'done',
  Closed = 'closed',
  Unknown = 'unknown',
}

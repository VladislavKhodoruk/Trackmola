export enum UserType {
  Employee = 'employee',
  CTO = 'cto',
  Manager = 'manager',
  Admin = 'admin',
}

export enum StateName {
  Dashboard = 'dashboard',
  Activity = 'activity',
  Profile = 'profile',
  Projects = 'projects',
  Reports = 'reports',
}
